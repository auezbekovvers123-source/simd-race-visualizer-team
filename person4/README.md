# Person 4 — Race canvas / graphics

Owner: @aninakit

 Files: race_[canvas.py](http://canvas.py) 

RaceCanvasDrawer — main drawing manager for rendering steps on a Tkinter Canvas GridLayout — simple geometry layout helper for the memory cells 

draw_grids — displays scalar grid (1 active element) and SIMD grid (parallel rows/chunks) draw_track — dual-lane racetrack featuring synchronized progress for both cars 

Tkinter Canvas logic is strictly confined to this package.